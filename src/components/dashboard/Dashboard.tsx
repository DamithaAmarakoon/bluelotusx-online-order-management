import * as React from 'react';
import styles from './Dashboard.module.scss';

const Dashboard = (): JSX.Element => {
  return <h1 className='text-center mt-5'>DASHBOARD</h1>;
};

export default Dashboard;
